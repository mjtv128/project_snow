<p id="notice"><%= notice %></p>

<h1>All Resorts</h1>

<table>
    <thead>
    <tr>
    <th>Resorts</th>
    </tr>
    </thead>

    <tbody>
    <% @resorts.each do |resort| %>
    <tr>
    <td><%= link_to resort.resort_name, resort_path(resort.id) %>
    </tr>
    <% end %>
    </tbody>
</table>
